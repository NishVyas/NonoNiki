import { NonoNikiValidators } from './nono-niki-validators';

describe('NonoNikiValidators', () => {
  it('should create an instance', () => {
    expect(new NonoNikiValidators()).toBeTruthy();
  });
});
